[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "github-issue-agent"
version = "0.1.0"
description = "An agentic system to resolve GitHub issues automatically."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
